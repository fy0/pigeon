{
package altentry

type ParserCustomData struct {
}
}

Entry1 <- A C EOF {
  return string(c.text)
}

Entry2 <- B C EOF {
  return string(c.text)
}

Entry3 <- C EOF {
  return string(c.text)
}

A <- 'a'+
B <- 'b'+
C <- 'c'+ {
  return string(c.text)
}

EOF ← !.

