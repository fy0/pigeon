{
package andnot

type ParserCustomData struct {}
}

Input ← _ AB _ EOF

AB ← abees:<[ab]+> &{ return strings.HasSuffix(abees.(string), "b") } / CD
CD ← ceedees:<[cd]+> !{ return strings.HasSuffix(ceedees.(string), "c") }

_ ← [ \t\n\r]*
EOF ← !.

